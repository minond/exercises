--[[ Usual veriable name rules apply. Reserved words include:

      and       break     do        else          elseif
      end       false     for       function      if
      in        local     nil       not           or
      repeat    return    then      true          until
      while

--]]


-- This multiline comment style allows for a single hyphen to be added to the
-- opening block in order to turn the content back into code.
